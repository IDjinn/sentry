# Secret — sensitive values. Fill in real values via:
#   kubectl create secret generic sentry-secrets -n sentry \
#     --from-literal=cf-token=xxx --from-literal=cf-zone=yyy --from-literal=llm-key=zzz
apiVersion: v1
kind: Secret
metadata:
  name: sentry-secrets
  namespace: sentry
type: Opaque
stringData:
  cf-token: REPLACE_ME
  cf-zone: REPLACE_ME
  llm-key: REPLACE_ME
  pg-password: changeme